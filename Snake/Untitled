class Animal(object):
    def __init__(self,name,health):
        self.name = name
        self.health = health
    def walk(self,health):
        self.health += 1
        return self
    def run(self,health):
        self.health += 5
        return sefl


Animal1 = ("Stacy", 100)
Animal1.walk().run()
